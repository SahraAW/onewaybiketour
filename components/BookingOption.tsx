export function BookingOption({ selected, title, description, meta, onClick }: Readonly<{ selected: boolean; title: string; description?: string; meta?: string; onClick: () => void }>) {
  return <button type="button" onClick={onClick} className={`w-full rounded-[1.25rem] border p-4 text-left transition-colors ${selected ? "border-orange bg-orange/10" : "border-ink/15 bg-white/25 hover:border-orange/60"}`}><div className="flex min-w-0 items-start justify-between gap-4"><span className="min-w-0 break-words"><strong className="block text-sm">{title}</strong>{description && <span className="mt-1 block break-words text-xs leading-relaxed text-ink/55">{description}</span>}</span>{meta && <span className="shrink-0 text-xs font-bold text-orange">{meta}</span>}</div></button>;
}
